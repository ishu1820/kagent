# Import the 'start' function from the greetings module
# inside the kagent.cli package
from kagent.cli.greetings import start

# Call the start() function
# This will execute whatever logic is defined inside the start function (starting the CLI interaction)
start()